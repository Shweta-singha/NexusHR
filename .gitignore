target/
*.class
*.jar
*.war
.idea/
*.iml
.classpath
.project
.settings/
*.log
.env

# Local-only pg_dump backups (e.g. taken before switching the local Postgres
# image) - contain real local dev data, never committed
db_backups/